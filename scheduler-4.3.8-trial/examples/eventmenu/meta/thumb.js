/* global scheduler */
scheduler.features.eventMenu.showContextMenuFor(scheduler.eventStore.first);

// raise flag for thumb generator indicating page is ready for taking screenshot
window.__thumb_ready = true;
